const {
  readActorAuthorization,
  readActorProfile,
  readAuthoritativeClaims,
  getTrustedActor,
  hasPermission,
  requirePermission,
  hasSystemRole,
  requireSystemRole,
  isRootActor,
  requireRootActor,
} = require('./auth')

module.exports = {
  readActorAuthorization,
  readActorProfile,
  readAuthoritativeClaims,
  getTrustedActor,
  hasPermission,
  requirePermission,
  hasSystemRole,
  requireSystemRole,
  isRootActor,
  requireRootActor,
}
